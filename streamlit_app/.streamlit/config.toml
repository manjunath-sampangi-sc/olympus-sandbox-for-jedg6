[global]

# By default, Streamlit checks if the Python watchdog module is available and, if not,
# prints a warning asking for you to install it. The watchdog module is not required,
# but highly recommended. It improves the user experience by reducing latency.
disableWatchdogWarning = false

# If this is set to false, Streamlit will not open a browser window on startup.
openBrowserOnStartup = true

# By default, Streamlit displays a warning when a user sets both a widget default
# value in the function defining the widget and a widget value via the widget's key
# in st.session_state.
showWarningOnDirectExecution = true

[logger]

# Level of logging: 'error', 'warning', 'info', or 'debug'.
level = "info"

# String format for logging messages. If logger.datetimeFormat is set, logger messages
# will default to `%(asctime)s.%(msecs)03d %(name)s: %(levelname)s: %(message)s`.
# See [Python's documentation](https://docs.python.org/2.6/library/logging.html#formatter-objects)
# for available attributes.
messageFormat = "%(asctime)s %(name)s: %(levelname)s: %(message)s"

[client]

# Whether to enable st.cache.
caching = true

# If false, makes your Streamlit script not draw to a
# Streamlit app.
displayEnabled = true

# Controls whether uncaught app exceptions are displayed in the browser.
# By default, this is set to True and Streamlit displays app exceptions and
# associated tracebacks in the browser.
showErrorDetails = true

[browser]

# Internet address where users should point their browsers in order to
# connect to the app. Can be IP address or DNS name and path.
# This is used to: 
# - Set the correct URL for CORS and XSRF protection purposes.
# - Show the URL on the terminal
# - Open the browser
# Default: 'localhost'
serverAddress = "localhost"

# Whether to send usage statistics to Streamlit.
gatherUsageStats = false

# Port where users can connect to the app.
serverPort = 8501

[server]

# List of folders that should not be watched for changes. This
# impacts both "Run on Save" and @st.cache.
# Relative paths will be taken as relative to the current working directory.
# Example: ['/home/user1/env', 'relative/path/to/folder']
folderWatchBlacklist = []

# Change the type of file watcher used by Streamlit, or turn it off
# completely.
# Allowed values: * "auto" : Streamlit will attempt to use the
#                             watchdog module, and falls back to
#                             polling if watchdog is not available.
#                 * "watchdog" : Force Streamlit to use the watchdog module.
#                 * "poll" : Force Streamlit to always use polling.
#                 * "none" : Streamlit will not watch files.
fileWatcherType = "auto"

# Symmetric key used to produce signed cookies. If deploying on multiple replicas,
# ensure that this is set to the same value across all replicas to ensure they all
# share the same secret.
cookieSecret = "olympus-analytics-demo-secret-key-2024"

# If false, will attempt to open a browser window on start.
headless = false

# Automatically rerun script when the file is modified on disk.
runOnSave = true

# The address on which the server will listen for client and browser connections.
# Use this if you want to bind the server to a specific address. If set, the server
# will only be accessible from this address, and not from any aliases (like localhost).
address = "0.0.0.0"

# Server port number.
port = 8501

# Enables support for Cross-Origin Resource Sharing (CORS) protection, for added security.
# Due to conflicts between CORS and XSRF, if `server.enableXsrfProtection` is on and
# `server.enableCORS` is off at the same time, we will prioritize `server.enableXsrfProtection`.
enableCORS = true

# Enables support for Cross-Site Request Forgery (XSRF) protection, for added security.
# Due to conflicts between CORS and XSRF, if `server.enableXsrfProtection` is on and
# `server.enableCORS` is off at the same time, we will prioritize `server.enableXsrfProtection`.
enableXsrfProtection = true

# Max size, in megabytes, for files uploaded through the file_uploader.
maxUploadSize = 200

# Max size, in megabytes, of messages that can be sent via the WebSocket connection.
maxMessageSize = 200

# Enables support for websocket compression.
enableWebsocketCompression = false

[theme]

# Primary accent color for interactive elements.
primaryColor = "#1f4e79"

# Background color for the main content area.
backgroundColor = "#ffffff"

# Background color used for the sidebar and most interactive widgets.
secondaryBackgroundColor = "#f0f2f6"

# Color used for almost all text.
textColor = "#262730"

# Font family for all text in the app, except code blocks. One of "sans serif", "serif", or "monospace".
font = "sans serif"